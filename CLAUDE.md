# Claude Code 开发规范

## API 接口文档规范

### 规则说明
每当创建或修改包含后台请求的前端页面时，必须在根目录的 `apidoc.md` 文件中补充相应的API接口文档信息。
UTF-8格式

### 文档要求
1. **接口基本信息**
   - 接口名称和描述
   - HTTP方法（GET/POST/PUT/DELETE等）
   - 接口路径
   - 接口用途说明

2. **请求参数**
   - 参数名称和类型
   - 是否必填
   - 参数说明和示例
   - 请求体格式（JSON/FormData等）

3. **响应信息**
   - 成功响应格式和字段说明
   - 错误响应格式和错误码
   - 响应数据示例
   - 数据结构说明

4. **开发注意事项**
   - 权限验证要求
   - 数据校验规则
   - 业务逻辑约束
   - 性能优化建议

5. **AI后端开发提示**
   - 技术栈建议
   - 数据库设计要求
   - 业务流程说明
   - 集成指导

### 目的
- 为AI生成后端代码提供准确的接口规范
- 确保前后端开发的一致性
- 提高开发效率和代码质量
- 便于维护和后续开发

### 执行时机
- 新增带有API调用的页面/组件后
- 修改现有接口调用逻辑后
- 页面功能更新涉及后端交互时

参考文档：@apidoc.md
- 每次都需要编译后才能说完成